# P02 Bootstrap Operator Interface

Status: artifact-emitted working slice. Full `cargo test` verification passed for current tree. Phase remains open (no global closure).

This document indexes the deterministic command surface, workflows, examples, acceptance gates, and receipts for P02-018.
